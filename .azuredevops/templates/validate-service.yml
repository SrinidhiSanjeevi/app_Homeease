# ============================================================
# Reusable validation steps for one Node service.
#
# Order matters: cheapest and most likely to fail goes first, so a
# broken lint doesn't wait 4 minutes behind a test suite.
# ============================================================

parameters:
  - name: name
    type: string
  - name: path
    type: string
  - name: hasTests
    type: boolean
    default: true
  - name: hasLint
    type: boolean
    default: true
  - name: sonarProjectKey
    type: string
  - name: sonarHostUrl
    type: string
  - name: nodeVersion
    type: string
    default: '22.x'
  - name: trivyVersion
    type: string
    default: '0.74.0'

steps:

  - task: UseNode@1
    displayName: 'Node.js ${{ parameters.nodeVersion }} — ${{ parameters.name }}'
    inputs:
      version: '${{ parameters.nodeVersion }}'

  # Cache the npm download cache, not node_modules. Caching node_modules
  # can restore a tree that doesn't match the lockfile after a dependency
  # bump; ~/.npm is content-addressed and always safe.
  - task: Cache@2
    displayName: 'Cache npm — ${{ parameters.name }}'
    inputs:
      key: 'npm | "$(Agent.OS)" | ${{ parameters.path }}/package-lock.json'
      restoreKeys: |
        npm | "$(Agent.OS)"
      path: '$(Pipeline.Workspace)/.npm'

  - script: |
      set -euo pipefail
      npm config set cache "$(Pipeline.Workspace)/.npm"
      cd ${{ parameters.path }}
      npm ci --no-audit --no-fund
    displayName: 'npm ci — ${{ parameters.name }}'

  - ${{ if eq(parameters.hasLint, true) }}:
      - script: |
          set -euo pipefail
          cd ${{ parameters.path }}
          npm run lint
        displayName: 'Lint — ${{ parameters.name }}'

  # npm audit reads the lockfile and knows about advisories Trivy's
  # database may lag on. Running both is deliberate — they disagree.
  - script: |
      set -euo pipefail
      cd ${{ parameters.path }}
      npm audit --audit-level=high --omit=dev
    displayName: 'npm audit (high+) — ${{ parameters.name }}'

  - ${{ if eq(parameters.hasTests, true) }}:
      - script: |
          set -euo pipefail
          cd ${{ parameters.path }}
          npm run test:coverage -- --ci --reporters=default --reporters=jest-junit
        displayName: 'Test + coverage — ${{ parameters.name }}'
        env:
          JEST_JUNIT_OUTPUT_DIR: '$(Build.ArtifactStagingDirectory)/${{ parameters.name }}'
          JEST_JUNIT_OUTPUT_NAME: 'junit.xml'

      - task: PublishTestResults@2
        displayName: 'Publish test results — ${{ parameters.name }}'
        condition: succeededOrFailed()
        inputs:
          testResultsFormat: 'JUnit'
          testResultsFiles: '$(Build.ArtifactStagingDirectory)/${{ parameters.name }}/junit.xml'
          testRunTitle: '${{ parameters.name }}'
          failTaskOnFailedTests: true

      - task: PublishCodeCoverageResults@2
        displayName: 'Publish coverage — ${{ parameters.name }}'
        condition: succeededOrFailed()
        inputs:
          summaryFileLocation: '${{ parameters.path }}/coverage/cobertura-coverage.xml'
          pathToSources: '${{ parameters.path }}'

  # Frontend builds instead of testing. It still has to compile.
  - ${{ if eq(parameters.hasTests, false) }}:
      - script: |
          set -euo pipefail
          cd ${{ parameters.path }}
          npm run build
        displayName: 'Production build — ${{ parameters.name }}'

  # ──────────────────────────────────────────────────────────
  # SCA on the source tree.
  #
  # This is NOT the same as the image scan in the Package stage:
  # here Trivy sees devDependencies and the lockfile; there it sees
  # only what shipped. Both matter — a vulnerable build-time dep is
  # a supply-chain risk even if it never reaches production.
  # ──────────────────────────────────────────────────────────

  - task: Cache@2
    displayName: 'Cache Trivy DB'
    inputs:
      key: 'trivy-db | "$(Agent.OS)" | "${{ parameters.trivyVersion }}" | "$(Build.BuildId)"'
      restoreKeys: |
        trivy-db | "$(Agent.OS)" | "${{ parameters.trivyVersion }}"
        trivy-db | "$(Agent.OS)"
      path: '$(Pipeline.Workspace)/.trivy-cache'

  - script: |
      set -euo pipefail
      if ! command -v trivy >/dev/null 2>&1; then
        curl -fsSL -o /tmp/trivy.deb \
          "https://github.com/aquasecurity/trivy/releases/download/v${{ parameters.trivyVersion }}/trivy_${{ parameters.trivyVersion }}_Linux-64bit.deb"
        sudo dpkg -i /tmp/trivy.deb
      fi
      trivy --version

      trivy fs \
        --cache-dir "$(Pipeline.Workspace)/.trivy-cache" \
        --scanners vuln,secret,misconfig \
        --severity HIGH,CRITICAL \
        --ignore-unfixed \
        --exit-code 1 \
        --format table \
        ${{ parameters.path }}
    displayName: 'Trivy fs — ${{ parameters.name }}'

  # ──────────────────────────────────────────────────────────
  # SonarQube — explicit SonarScanner CLI, invoked directly instead
  # of through SonarQubePrepare@8 / SonarQubeAnalyze@8.
  #
  # Those tasks hand the scanner its config through an opaque
  # SONARQUBE_SCANNER_PARAMS env var. Whenever that handoff isn't
  # exactly right, the bundled scanner's newer "Scanner Engine"
  # bootstrap silently defaults to SonarQube CLOUD instead of our
  # self-hosted server — which is what kept happening here, however
  # the env var was massaged. Running the scanner ourselves with
  # explicit -D flags removes that failure class entirely: every
  # property it needs is passed on the command line, nothing is
  # inferred or handed off through an intermediate variable.
  #
  # Deliberately pinned to scanner-cli 5.0.1.3006 — the last stable
  # line before that CI-vendor-autodetecting "Scanner Engine" was
  # introduced. It talks plain HTTP to the SonarQube Web API (fine
  # for our 9.9.8 Community Build) and has no branch/PR autodetection
  # to fight, so sonar.branch.name is simply never generated — the
  # "Developer Edition required" error can't happen either.
  #
  # sonar.projectKey comes from each service's own sonar-project.properties.
  # project.settings and sonar.projectBaseDir are NOT combined (SonarQube
  # 9.9 docs: they're incompatible) — instead we cd into the service
  # directory and let the scanner use its own default: current working
  # directory as project base dir, reading ./sonar-project.properties
  # from there directly, no explicit -D flags needed for either.
  #
  # SONAR_TOKEN is a secret in the homeease-ci variable group — ADO
  # does not auto-inject secret variables into a script's environment,
  # so it must be mapped explicitly via env: below. SONAR_HOST_URL is
  # not a credential (see azure-pipelines.yml), passed in as a
  # parameter instead.
  # ──────────────────────────────────────────────────────────

  - script: |
      set -euo pipefail
      if ! command -v sonar-scanner >/dev/null 2>&1; then
        curl -fsSL -o /tmp/sonar-scanner.zip \
          "https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-5.0.1.3006-linux.zip"
        unzip -q /tmp/sonar-scanner.zip -d /tmp
        sudo mv /tmp/sonar-scanner-5.0.1.3006-linux /opt/sonar-scanner
        sudo ln -sf /opt/sonar-scanner/bin/sonar-scanner /usr/local/bin/sonar-scanner
      fi
      sonar-scanner --version

      echo "SONAR_TOKEN present: $([ -n "${SONAR_TOKEN:-}" ] && echo YES || echo NO)"
      echo "SONAR_HOST_URL: ${SONAR_HOST_URL:-<not-set>}"

      cd "$(Build.SourcesDirectory)/${{ parameters.path }}"

      sonar-scanner \
        -Dsonar.host.url="${{ parameters.sonarHostUrl }}" \
        -Dsonar.token="$SONAR_TOKEN"
    displayName: 'Sonar scan — ${{ parameters.name }}'
    env:
      SONAR_TOKEN: $(SONAR_TOKEN)
      SONAR_HOST_URL: ${{ parameters.sonarHostUrl }}